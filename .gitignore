test/export.svg
test/export.png
test/export-sheet-*.svg
test/export-sheet-*.png
packet.pdf
node_modules/

# derived rasters from `npm run approve` — look at them, don't commit them.
# the .svg goldens beside them ARE tracked.
test/approved/*.png
